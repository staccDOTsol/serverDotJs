"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const anchor_1 = require("@project-serum/anchor");
const spl_token_1 = require("@solana/spl-token");
const programIds_1 = require("../../constants/programIds");
const file_1 = require("../../utils/file");
async function createMint(connection, walletKeypair) {
    const mint = await spl_token_1.Token.createMint(connection, walletKeypair, walletKeypair.publicKey, walletKeypair.publicKey, 9, programIds_1.TOKEN_PROGRAM_ID);
    return mint.publicKey;
}
;
async function main() {
    const process = require("process");
    const walletKeyPair = (0, file_1.loadWalletKey)(process.env.KEYPAIR);
    const solConnection = new anchor_1.web3.Connection(process.env.SOLANA_CLUSTER);
    return createMint(solConnection, walletKeyPair);
}
main().then((mint) => { console.log(mint.toBase58()); });
